# LSRP Scroot

## Overview

`lsrp_scroot` is currently a placeholder resource.

At the moment it does not contain active client or server behavior.

## Current Status

- `client/client.lua` is empty.
- `server/server.lua` is empty.

## Intended Use

This can be kept as a reserved resource for future shared hooks, script bootstrap code, or project-specific experimentation.

## Notes

- It is safe to treat this resource as unimplemented until real behavior is added.
- If this resource starts to take on real responsibility, expand this README with the actual ownership and purpose.